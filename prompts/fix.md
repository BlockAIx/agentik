# Fix failing tests — {{TASK}}

The implementation exists but the following tests are failing. Fix **only** the
broken code; do not refactor or rename unrelated parts.

## {{TEST_LABEL}} output

```
{{TRUNCATED_LOGS}}
```
