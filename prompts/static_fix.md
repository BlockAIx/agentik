# Static analysis failures — {{TASK}}

Tests pass but these type-check/lint errors must be fixed. Fix **only** reported issues — no refactoring, no logic changes, no signature changes beyond what's needed.

After fixes, re-run `get_errors` (VS Code) or the same CLI command and confirm zero errors. Do not finish until clean.

```
{{TRUNCATED_LOGS}}
```
