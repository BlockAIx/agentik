````markdown
# Milestone review fix — {{TASK}}

The milestone review identified issues that must be fixed before tagging.
Fix **only** the issues listed below — no refactoring, no new features,
no unrelated changes.

After fixes, run `get_errors` (VS Code) or the ecosystem CLI linter and
confirm zero errors. Do not finish until clean.

## Milestone verdict

```
{{MILESTONE_REPORT}}
```

````
